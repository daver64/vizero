#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int argc, char** argv) {
    printf("Hello from clangd test!\n");
    
    // Test completion here
    // Position cursor after 'str' and press Ctrl+Space
    str
    
    // Test member completion - position after '.' and press Ctrl+Space
    FILE* fp = fopen("test.txt", "r");
    fp.
    
    char* message = "Testing clangd completion";
    printf("%s\n", message);
    
    return 0;
}